# API Configuration for Dashboard
# Leave empty to use relative /api (assumes same-origin deployment)
# Set to http://localhost:3000 for dev with Next.js API routes
# Set to http://localhost:3001 for dev with real backend
VITE_API_URL=
