# Supabase
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# Only needed for admin operations (not exposed to client)
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key

# App URL (used for OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# API Configuration
# Leave empty to use serverless (Next.js API routes on same origin)
# Set to http://localhost:3001 to use real backend in dev
# Set to https://api.kairo.alejojamc.com for external backend in prod
NEXT_PUBLIC_API_URL=

# Dashboard Configuration
# Leave empty to use /dashboard (same origin, for production)
# Set to http://localhost:5173/dashboard for dev with Vite
NEXT_PUBLIC_DASHBOARD_URL=
