# ==========================================
# Supabase Configuration
# ==========================================
# The URL of your Supabase project API (used by the client to connect to your database)
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co

# The public anon key for your Supabase project (safe to expose to client, enforces RLS policies)
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key

# The default publishable key for Supabase services
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_DEFAULT_KEY=your-publishable-key

# ==========================================
# Google OAuth Configuration
# ==========================================
# The Client ID from Google Cloud Console used for Google Sign-In (safe to expose to client)
NEXT_PUBLIC_GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com

# The Client Secret from Google Cloud Console used to exchange auth codes for tokens (NEVER expose to client)
GOOGLE_CLIENT_SECRET=your-google-client-secret

# ==========================================
# Application URLs
# ==========================================
# The base URL of this application (used for generating absolute links and OAuth redirects)
NEXT_PUBLIC_APP_URL=http://localhost:3000

# The base URL of the dashboard application where users are redirected after login
# Leave empty to use /dashboard (same origin, for production)
# Set to http://localhost:5173/dashboard/ for local development with Vite
NEXT_PUBLIC_DASHBOARD_URL=http://localhost:5173/dashboard/

# ==========================================
# API Configuration
# ==========================================
# Leave empty to use serverless Next.js API routes on the same origin
# Set to http://localhost:3001 to use a local backend during development
# Set to https://api.kairo.alejojamc.com for an external backend in production
NEXT_PUBLIC_API_URL=
