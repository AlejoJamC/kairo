---
description: MCP integrations and subagent usage
---

## Linear (MCP)
When a Linear issue is referenced, its description is the spec. Acceptance criteria in Linear = done criteria.

## Subagents
- Run a read-only explore pass before implementing to map affected files
- Use plan mode (shift+tab) for any spec touching more than 3 files
