{
  "name": "kairo-monorepo",
  "private": true,
  "workspaces": ["apps/*", "packages/*"],
  "scripts": {
    "dev": "turbo run dev --filter=@kairo/webapp --filter=@kairo/landing --filter=@kairo/api",
    "build": "turbo run build",
    "lint": "turbo run lint",
    "clean": "turbo run clean"
  },
  "devDependencies": {
    "turbo": "^2.3.3",
    "typescript": "~5.9.3"
  },
  "packageManager": "bun@1.3.5",
  "engines": {
    "node": ">=20",
    "bun": ">=1.0.0"
  }
}
