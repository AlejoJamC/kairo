/**
 * The predictions file inside a run directory.
 *
 * The name used to carry the corpus size, which was descriptive when there was
 * one corpus of fifty emails and stopped being true the moment a second one
 * existed with forty-five. A count in a filename is a claim the file cannot
 * keep. The ground truth of the original corpus keeps its own name.
 */
export const PIPELINE_OUTPUT = 'pipeline_output.csv';
