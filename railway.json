{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install && bun run --filter=@kairo/intelligence build && bun run --filter=@kairo/identity build"
  },
  "deploy": {
    "startCommand": "bun run apps/api/src/index.ts",
    "healthcheckPath": "/api/v1/health"
  }
}
