{
  "$schema": "https://railway.com/railway.schema.json",
  "build": {
    "builder": "RAILPACK",
    "buildCommand": "bun install && bunx turbo run build:api --filter=@kairo/api"
  },
  "deploy": {
    "startCommand": "bun run apps/api/dist/index.js",
    "healthcheckPath": "/api/v1/health"
  }
}
